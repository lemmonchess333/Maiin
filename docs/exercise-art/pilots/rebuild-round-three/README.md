# Third pass: changed-camera rebuilds

Rebuilt masters for three existing unresolved exercises. One complete six-frame curl draft was produced; the other two were stopped at their failed quality gates. None is release-approved.

| Exercise | Result | Remaining issue |
| --- | --- | --- |
| Reverse barbell curl | New symmetric front master and six native files from three distinct poses; clean background and readable start/top wrists | Midpoint forearm axis is hidden by foreshortening, so neutral wrist alignment is still unverified |
| Dumbbell RDL | New side-view master with improved headroom and calf colouring; one selected frame | Halfway pose moves fixed shoes/floor line; correction did not restore anchors, so no bottom or complete set generated |
| Reverse flyes | Two rejected rear-view master attempts preserved | Both fail the required near-parallel hip hinge, with secondary symmetry/rotation issues; no downstream frames generated |

Exact prompts, references, cues, image hashes and per-exercise reviews are saved alongside the imagery. Curl endpoint/return reuse is explicit. RDL cues are planned beats only, not evidence of a completed sequence. See INDEPENDENT_REVIEW.md for independent findings against the actual images.

All 16 PNGs decoded. Lint, build and all 9,697 active tests passed, with 347 skipped. These checks ran against the preserved local source snapshot and do not validate exercise mechanics. Sequential app/mobile playback remains unverified. Only this folder is published; existing artwork and unrelated local rebuild changes are preserved.
