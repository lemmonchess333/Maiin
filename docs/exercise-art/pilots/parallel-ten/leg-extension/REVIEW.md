# Leg extension — six-frame candidate requiring rework

Six full-resolution native 1536×1024 PNGs are saved, four unique configurations. Frames 5/6 reuse 3/2 for mechanically matching controlled return poses. All edits used anatomy-v3 identity plus the exercise master, with adjacent poses supplemental.

The seated athlete extends knees through a monotonic sequence; torso, hips, handles, seat and base are visually stable. White trainers and quadriceps colour remain consistent. The master depicts a plate-loaded machine, a valid machine variation, so there is no cable or weight-stack routing to simulate. The cue about not crashing a stack becomes a generic controlled-load cue.

FAILED MECHANICAL GATE: circular machine axle intended to remain fixed at approximately (555,462) drifts to approximately (525,411) at top extension. Intermediate poses also shift it. This makes the supposedly fixed linkage move and changes apparent lever geometry. These images are not approved for public instructional use.

A targeted axle-only correction was attempted. It was blocked by image safety at output, category sexual, request c495dcbf-130b-49e2-889d-e6a3ef2e0e12. No unchanged retry or alternative clothing substitution was attempted. Original candidate frames remain for transparent review.

Actual app playback and mobile light/dark evidence are unverified. Parent reports local browser review blocked with net::ERR_BLOCKED_BY_CLIENT. No public files, registry, or player references changed. Parent code validation: lint/build passed and 9697 tests passed; those gates do not approve image mechanics.
