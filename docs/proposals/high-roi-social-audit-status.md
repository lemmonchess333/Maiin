# High-ROI Programme/Body/Social audit — verification status

**Source:** operator packet `high-roi-programme-body-composition-social-audit.md`
(2026-07-10 audit + a ledger of items "implemented locally, pending
commit" dated up to 2026-07-17). **Verified against `main` 2026-07-16.**

Same failure mode as the Form-rig packets: the audit's _product
analysis_ is sound, but its _implementation ledger_ describes an
uncommitted worktree that never reached this repo. Treat the design
content as a backlog and the status content as void.

## Verified ON main (do not rebuild)

| Audit item                           | Evidence on main                                                                                                                         |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| GOALS-CORE-01 (Circles/goalSpaces)   | `src/features/goalSpace/*`, `functions/lib/goalSpaceMembership.js`, rules + deletion inventory                                           |
| PROGRAM-BLOCK-01 (Training Blocks)   | `users/{uid}/trainingBlocks` in PROTECTED_PATHS, `useTrainingBlock.ts`, TrainingBlockCard                                                |
| PROGRAM-FLEX-01 (Express Sessions)   | `src/features/program/expressSession.ts`                                                                                                 |
| CHECKIN-01 (Momentum Check-in)       | `src/components/review/MomentumCheckinCard.tsx`, `users/{uid}/checkins/{weekKey}`                                                        |
| BODY-VAULT-00/01 (Progress Vault)    | `users/{uid}/progressCheckins`, vault links                                                                                              |
| NUTR-CONSISTENCY-01                  | `users/{uid}/nutritionCommitments/{weekKey}`                                                                                             |
| ROUTINE-EXCHANGE-01 foundation       | `savedRoutines.ts` blueprint/weight-redaction contract, `routine_shared` event kind                                                      |
| LIFT-01 (draft identity)             | `useWorkoutDraft.ts` identity/fingerprint scope                                                                                          |
| PROGRAM-SESSION-ORDER-01 **backend** | `nextWorkoutOverride` field + `setNextWorkout` writer + reducer command + sanitizer allowlist — **UI was never wired; wired in this PR** |

## Orphaned (claimed local, NOT on main) — ranked backlog

| Rank       | Item                                                                      | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------- | ------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| shipped    | PROGRAM-SESSION-ORDER-01 UI ("Make This Next" / "Follow Programme Order") | Closed by this PR — pure wiring onto the existing tested writer.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| superseded | SOCIAL-CIRCLE-DEFAULT-01                                                  | **Satisfied by SOCIAL-HOME-01 (#1651), 2026-07-18.** Together (Circles-first) is now the unconditional default surface for every user and leads with the active Circle; Find/People was demoted to a header-triggered overlay and is no longer a default landing surface. The specific bug this row targeted — the old `Soc5c` new-user redirect to People — was deleted outright (`Social.tsx` "old Soc5c smart default … is gone"). No routing fix required.                                           |
| shipped    | CIRCLE-INVITE-ACTIVATION-01 (+ its CIRCLE-PULSE-01 dependency)            | Shipped #1652 — post-create invite hand-off + one-member "Your Circle is ready" state; the pulse arrived with SOCIAL-FOCUS-01.                                                                                                                                                                                                                                                                                                                                                                           |
| shipped    | RACE-EVENT-IDENTITY-01 (optional race name)                               | Shipped #1659 — optional `raceGoal.eventName` threaded end-to-end (profile → programState mirror → cockpit heading) with the `profileSanitizer` sub-field allow-list + the ProgrammeSettings drop-site fix; deployed 2026-07-18.                                                                                                                                                                                                                                                                         |
| shipped    | RACE/BLOCK "Train together" handoffs + CIRCLE-CONTEXT-01                  | Shipped #1653 — plan→Circle prefill contract from Training Blocks + Race cockpit.                                                                                                                                                                                                                                                                                                                                                                                                                        |
| shipped    | SOCIAL-FOCUS-01 (Circle Weekly Focus — operator handout 2026-07-17)       | Server-owned weekly check-in (`goalSpaceWeeklyCheckIn`, deterministic `${uid}_${weekKey}` event) + optional closed-enum `weeklyFocus` + `backGoalSpaceCheckIn` response loop with the generic `circle_focus_backed` notification. Delivers the SUPPORT-ACK-class acknowledgement mechanic (rank 6) scoped to focus check-ins; rank 6's remaining items shipped 2026-07-18 (target lifecycle #1662, activity notifications #1663 — see the rows above). The GsPb1 plan-file row carries the STATUS entry. |
| shipped    | CIRCLE-SESSION-01 / CIRCLE-RUN-SESSION-01                                 | Shipped #1655 — explicit summary-only `session_completed` share after planned sessions.                                                                                                                                                                                                                                                                                                                                                                                                                  |
| shipped    | CIRCLE-TARGET-LIFECYCLE/CONTINUATION                                      | Shipped #1662 (deployed 2026-07-18) — owner-only `resolveGoalSpaceTarget` callable: `continue` (new future `targetDate`) / `wrap` (`active:false` + `endedAt`). Detection is client-side from the loaded `targetDate` (no sweep/index/scan); the featured-hero prompt renders at/after the date. `GoalSpace.endedAt` additive (no schema bump).                                                                                                                                                          |
| shipped    | ACTIVITY-NOTIFICATIONS                                                    | Shipped #1663 — `onGoalSpaceEventCreated` Firestore trigger (`TRIGGER_CAP`) fans a NAMED in-app notification to the ≤7 other members for `milestone`/`needs_support`/`joined`/`routine_shared` (excludes `session_completed` frequency + `weekly_check_in`'s own loop). Deterministic notification id `${spaceId}_${eventId}` → at-least-once idempotent; R1A-guarded; no push. Four `circle_*` types added server + client.                                                                             |
| shipped    | SUPPORT-ACK                                                               | Delivered by SOCIAL-FOCUS-01's `backGoalSpaceCheckIn` loop (focus check-ins). Not extended to acking milestone/needs_support events — a nicety, not a gap; deferred deliberately.                                                                                                                                                                                                                                                                                                                        |
| shipped    | ROUTE-PLANNING-01 (Mapbox `planRunningRoute`)                             | Shipped #1657 (rebased from `claude/route-planning`); `MAPBOX_DIRECTIONS_TOKEN` provisioned + first owner-deploy done 2026-07-18. Web build flag `VITE_ROUTE_PLANNING_ENABLED` enabled via #1658. Run11 Pro gate retained. Rollout: `docs/road-aware-route-planning.md`.                                                                                                                                                                                                                                 |

## Operator decisions queued

1. **Routing provider: ORS (per CLAUDE.md cost model) vs Mapbox (per this
   audit).** Conflicting locked-ish positions — needs an explicit call
   before any route-planning callable is built.
2. Circle-wave sequencing: confirm the rank above or reorder.
3. The audit's "Founder Decisions" table (naming, templates, entitlement,
   moderation) remains open where not already superseded by shipped work.
