# Tropos — Domain Context

Domain vocabulary as it solidifies. Skills like
`improve-codebase-architecture`, `diagnose`, and `tdd` read this to
use the project's actual terms instead of inventing synonyms.

Currently empty — populate via `/grill-with-docs` or by hand as the
language stabilises.
